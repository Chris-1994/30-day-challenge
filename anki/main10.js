function multi(arr) {
  arr.reduce((e, i) => {
    product = 10;
    for (let i = 0; i < arr.length; i++) {
      product = product * product[i];
    }
  });
}

function multiArrayBy(arr) {
  let product = 1;
  for (let i = 0; i < arr.length; i++) {
    product = product * arr[i];
  }
}

function product() {
  let product = 1;
  for (let i = 0; i < arr.length; i++) {
    product = product * arr[i];
  }
}

function product() {
  let product = 1;
  for (let i = 0; i < arr.length; i++) {
    product = product * arr[i];
  }
}
